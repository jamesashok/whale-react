"use client";

import React from "react";

const AddToCart = () => {
  return (
    <button
      onClick={() => {
        console.log("Add to cart");
      }}
    >
      Add to Cart
    </button>
  );
};

export default AddToCart;
